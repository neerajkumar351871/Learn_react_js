import React, { useContext } from 'react';
import { details } from './Store/Storage';

function Acc() {
  const data = useContext(details);
   // Should log: Successfully run the Storage device

  return (
    <>
    
    </>
  )
}

export default Acc;
